# ── Backend ─────────────────────────────────────────────────────
ADMIN_USER=admin
ADMIN_PASSWORD=          # Superadmin de secours (env Render)

APP_SECRET=              # Secret JWT (obligatoire en production, deja sur Render)

# ── Database (obligatoire sur Render pour garder les inscriptions) ──
DATABASE_URL=            # postgresql://user:pass@host:5432/db

# ── Dev local (optionnel) ───────────────────────────────────────
# Sans DATABASE_URL, les inscriptions sont stockées dans data/participants.json
# PORT=3001
